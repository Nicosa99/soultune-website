(function() {
    'use strict';
    // Use cases interaction logic
    // Currently relying on CSS hover states which are performant
    // Can be expanded for touch-device specific interactions (e.g. tap to glow)
})();
